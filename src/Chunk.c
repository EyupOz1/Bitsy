#include "raylib.h"
#include "Block.h"


/* Mesh chunk_mesh_create(Block* Chunk)
{

    for (int i = 0; i < CHUNK_DIMENSIONS; i++)
    {
        for (int j = 0; j < CHUNK_DIMENSIONS; j++)
        {
            for (int k = 0; k < CHUNK_DIMENSIONS; k++)
            {
                Chunk[i].coords
            }
        }
    }
} */